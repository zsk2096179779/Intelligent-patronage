# 拒绝原因功能实现说明

## 功能概述

已实现拒绝原因存储功能，当审核拒绝策略组合时，拒绝原因会存储到数据库的 `reject_reason` 字段中，并在查询接口中返回给前端。

## 需要执行的步骤

### 1. 执行数据库脚本（重要！）

在启动应用之前，**必须先执行 SQL 脚本**添加 `reject_reason` 字段：

**脚本位置：** `src/main/resources/sql/add_reject_reason_field.sql`

**SQL 内容：**
```sql
ALTER TABLE portfolios 
ADD COLUMN reject_reason VARCHAR(500) NULL COMMENT '审核拒绝原因' 
AFTER listed;
```

**执行方式：**

1. **使用数据库管理工具（推荐）：**
   - 连接到数据库：`116.62.82.244:3306/wealthadvisor`
   - 执行 `src/main/resources/sql/add_reject_reason_field.sql` 文件

2. **使用命令行：**
   ```bash
   mysql -h 116.62.82.244 -P 3306 -u remote_admin -p wealthadvisor < src/main/resources/sql/add_reject_reason_field.sql
   ```

3. **直接在数据库工具中执行：**
   ```sql
   ALTER TABLE portfolios 
   ADD COLUMN reject_reason VARCHAR(500) NULL COMMENT '审核拒绝原因' 
   AFTER listed;
   ```

### 2. 验证字段是否添加成功

执行以下 SQL 验证：
```sql
-- 查看表结构
DESC portfolios;

-- 或者
SHOW COLUMNS FROM portfolios LIKE 'reject_reason';
```

如果能看到 `reject_reason` 字段，说明添加成功。

### 3. 重启应用

执行 SQL 脚本后，重启 Spring Boot 应用即可使用新功能。

## 功能说明

### 数据流程

1. **前端发送拒绝请求：**
   ```json
   POST /api/strategy-combination/{id}/reject
   {
     "reason": "不符合审核标准"
   }
   ```

2. **后端处理：**
   - Controller 接收拒绝原因
   - Service 传递拒绝原因到 Mapper
   - Mapper 执行 SQL 更新 `listed = -1` 和 `reject_reason = '不符合审核标准'`

3. **数据库存储：**
   - `listed` 字段更新为 `-1`
   - `reject_reason` 字段存储拒绝原因

4. **查询返回：**
   - 查询接口会返回 `rejectReason` 字段
   - 前端可以根据 `listed = -1` 和 `rejectReason` 显示拒绝信息

### 业务逻辑

1. **审核拒绝时：**
   - `listed` 更新为 `-1`
   - `reject_reason` 存储拒绝原因

2. **审核通过时：**
   - `listed` 更新为 `1`
   - `reject_reason` 清空为 `NULL`（如果之前有拒绝原因）

3. **查询接口：**
   - 返回的数据中包含 `rejectReason` 字段
   - 如果组合被拒绝，`rejectReason` 会有值
   - 如果组合未拒绝或已通过，`rejectReason` 为 `null`

## 代码变更说明

### 1. 数据库变更
- 添加了 `reject_reason VARCHAR(500)` 字段

### 2. DTO 变更
- `PortfolioDetailDTO` 添加了 `rejectReason` 字段

### 3. Mapper 变更
- `rejectPortfolio` 方法添加了 `reason` 参数
- SQL 更新语句同时更新 `listed` 和 `reject_reason`
- `approvePortfolio` SQL 同时清空 `reject_reason`

### 4. Service 变更
- `rejectPortfolio` 方法现在会传递 `reason` 参数到 Mapper

### 5. 查询 SQL 变更
- 查询语句中添加了 `p.reject_reason` 字段
- ResultMap 中添加了 `reject_reason` 映射

## 测试建议

### 1. 测试审核拒绝
```bash
curl -X POST http://localhost:8080/api/strategy-combination/1/reject \
  -H "Content-Type: application/json" \
  -d '{"reason": "不符合审核标准"}'
```

### 2. 验证数据存储
```sql
SELECT id, name, listed, reject_reason 
FROM portfolios 
WHERE id = 1;
```

应该看到：
- `listed = -1`
- `reject_reason = '不符合审核标准'`

### 3. 测试查询接口
```bash
curl http://localhost:8080/combos
```

返回的数据中应该包含 `rejectReason` 字段。

### 4. 测试审核通过（清空拒绝原因）
```bash
curl -X POST http://localhost:8080/api/strategy-combination/1/approve
```

然后查询数据库，应该看到：
- `listed = 1`
- `reject_reason = NULL`

## 注意事项

1. **必须执行 SQL 脚本**：如果不执行 SQL 脚本添加字段，应用会报错（字段不存在）

2. **字段长度限制**：`reject_reason` 字段设置为 `VARCHAR(500)`，拒绝原因不能超过 500 个字符

3. **NULL 值处理**：
   - 未拒绝的组合：`reject_reason = NULL`
   - 已拒绝的组合：`reject_reason = '拒绝原因'`
   - 审核通过后：`reject_reason = NULL`（清空之前的拒绝原因）

4. **前端显示建议**：
   - 当 `listed = -1` 且 `rejectReason` 不为空时，显示拒绝原因
   - 当 `listed = 1` 时，不显示拒绝原因（即使有值也会被清空）

## 故障排查

### 问题：字段不存在错误

**错误信息：** `Unknown column 'reject_reason' in 'field list'`

**解决方案：** 执行 SQL 脚本添加字段

### 问题：拒绝原因未存储

**检查：**
1. 确认 SQL 脚本已执行
2. 检查 Mapper XML 中的 SQL 是否正确
3. 查看应用日志是否有错误

### 问题：查询接口未返回拒绝原因

**检查：**
1. 确认 DTO 中有 `rejectReason` 字段
2. 确认 ResultMap 中有映射
3. 确认查询 SQL 中包含了 `p.reject_reason`

