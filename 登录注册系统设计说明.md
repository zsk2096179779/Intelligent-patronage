# 登录注册系统设计说明

## 一、系统概述

本系统支持三种用户角色：
- **USER（普通用户）**：可以查看组合产品信息
- **STAFF（工作人员）**：可以创建组合产品、管理产品信息
- **AUDITOR（审核人员）**：可以审核组合产品（通过/拒绝）、创建组合产品

不同角色登录后进入不同的页面。

---

## 二、数据库设计

### 用户表（users）

执行以下SQL脚本创建用户表：

```sql
-- 文件位置：src/main/resources/sql/user_auth_schema.sql
```

**表结构：**
- `id`：用户ID（主键，自增）
- `username`：用户名（唯一）
- `password`：密码（BCrypt加密后）
- `email`：邮箱
- `phone`：手机号
- `role`：用户角色（ENUM: 'USER', 'STAFF', 'AUDITOR'）
- `status`：状态（1-正常，0-禁用）
- `created_at`：创建时间
- `updated_at`：更新时间

**测试账号：**
- 用户名：`user001`，密码：`123456`，角色：普通用户
- 用户名：`staff001`，密码：`123456`，角色：工作人员
- 用户名：`auditor001`，密码：`123456`，角色：审核人员

---

## 三、后端接口说明

### 1. 用户注册接口

**接口地址：** `POST /api/auth/register`

**请求体：**
```json
{
  "username": "testuser",
  "password": "123456",
  "email": "test@example.com",
  "phone": "13800138000"
}
```

**响应示例：**
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "id": 1,
    "username": "testuser",
    "email": "test@example.com",
    "phone": "13800138000",
    "role": "USER",
    "roleName": "普通用户"
  }
}
```

**注意：** 注册时默认角色为 `USER`（普通用户），工作人员和审核人员需要管理员在数据库中手动修改。

---

### 2. 用户登录接口

**接口地址：** `POST /api/auth/login`

**请求体：**
```json
{
  "username": "user001",
  "password": "123456"
}
```

**响应示例：**
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "id": 1,
    "username": "user001",
    "email": "user001@example.com",
    "phone": null,
    "role": "USER",
    "roleName": "普通用户"
  }
}
```

**说明：** 登录成功后，后端会在Session中存储用户信息（userId, username, role），前端无需手动存储。

---

### 3. 获取当前用户信息接口

**接口地址：** `GET /api/auth/user`

**响应示例：**
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": 1,
    "username": "user001",
    "email": "user001@example.com",
    "phone": null,
    "role": "USER",
    "roleName": "普通用户"
  }
}
```

**说明：** 如果未登录，返回401状态码。

---

### 4. 用户登出接口

**接口地址：** `POST /api/auth/logout`

**响应示例：**
```json
{
  "code": 200,
  "message": "登出成功",
  "data": null
}
```

---

### 5. 检查用户名是否可用接口

**接口地址：** `GET /api/auth/check-username?username=testuser`

**响应示例：**
```json
{
  "code": 200,
  "message": "检查完成",
  "data": {
    "available": true
  }
}
```

---

## 四、权限控制说明

### 后端权限拦截规则

1. **所有 `/api/**` 接口都需要登录**（除了以下接口）：
   - `/api/auth/login` - 登录
   - `/api/auth/register` - 注册
   - `/api/auth/check-username` - 检查用户名

2. **角色权限要求：**
   - **审核接口**（`/approve`, `/reject`）：需要 `AUDITOR` 角色
   - **创建组合产品接口**（`POST /api/strategy-combination`）：需要 `STAFF` 或 `AUDITOR` 角色
   - **其他接口**：所有已登录用户都可以访问

3. **权限不足时返回：**
   - 401：未登录
   - 403：权限不足

---

## 五、前端设计指南

### 1. 路由设计

建议使用以下路由结构：

```
/login                    # 登录页面（所有用户）
/register                 # 注册页面（所有用户）

# 普通用户路由
/user/dashboard           # 普通用户首页（查看组合产品列表）
/user/portfolio/:id       # 查看组合产品详情

# 工作人员路由
/staff/dashboard          # 工作人员首页（创建组合产品、管理产品）
/staff/portfolio/create   # 创建组合产品
/staff/portfolio/:id/edit # 编辑组合产品

# 审核人员路由
/auditor/dashboard        # 审核人员首页（待审核列表）
/auditor/review/:id       # 审核组合产品详情
```

### 2. 路由守卫（Route Guard）

在路由跳转前检查用户登录状态和角色：

```javascript
// 示例：Vue Router 路由守卫
router.beforeEach((to, from, next) => {
  // 获取当前用户信息
  const userInfo = store.getters.userInfo;
  
  // 公开路由（不需要登录）
  const publicRoutes = ['/login', '/register'];
  if (publicRoutes.includes(to.path)) {
    // 如果已登录，重定向到对应角色的首页
    if (userInfo) {
      const roleRoutes = {
        'USER': '/user/dashboard',
        'STAFF': '/staff/dashboard',
        'AUDITOR': '/auditor/dashboard'
      };
      next(roleRoutes[userInfo.role] || '/login');
    } else {
      next();
    }
    return;
  }
  
  // 需要登录的路由
  if (!userInfo) {
    next('/login');
    return;
  }
  
  // 根据角色检查权限
  const role = userInfo.role;
  const path = to.path;
  
  // 普通用户只能访问 /user/** 路由
  if (role === 'USER' && !path.startsWith('/user')) {
    next('/user/dashboard');
    return;
  }
  
  // 工作人员可以访问 /staff/** 和 /user/** 路由
  if (role === 'STAFF' && !path.startsWith('/staff') && !path.startsWith('/user')) {
    next('/staff/dashboard');
    return;
  }
  
  // 审核人员可以访问所有路由
  if (role === 'AUDITOR' && !path.startsWith('/auditor') && !path.startsWith('/staff') && !path.startsWith('/user')) {
    next('/auditor/dashboard');
    return;
  }
  
  next();
});
```

### 3. 状态管理（State Management）

建议使用 Vuex/Pinia（Vue）或 Redux（React）管理用户状态：

```javascript
// 示例：Vuex Store
const store = {
  state: {
    userInfo: null,  // 当前登录用户信息
    isLoggedIn: false
  },
  
  mutations: {
    SET_USER_INFO(state, userInfo) {
      state.userInfo = userInfo;
      state.isLoggedIn = !!userInfo;
    },
    
    CLEAR_USER_INFO(state) {
      state.userInfo = null;
      state.isLoggedIn = false;
    }
  },
  
  actions: {
    async login({ commit }, { username, password }) {
      const response = await api.post('/api/auth/login', { username, password });
      if (response.data.code === 200) {
        commit('SET_USER_INFO', response.data.data);
        return true;
      }
      throw new Error(response.data.message);
    },
    
    async logout({ commit }) {
      await api.post('/api/auth/logout');
      commit('CLEAR_USER_INFO');
    },
    
    async getCurrentUser({ commit }) {
      try {
        const response = await api.get('/api/auth/user');
        if (response.data.code === 200) {
          commit('SET_USER_INFO', response.data.data);
        }
      } catch (error) {
        commit('CLEAR_USER_INFO');
      }
    }
  }
};
```

### 4. API 请求拦截器

配置 axios/fetch 拦截器，自动处理 Session：

```javascript
// 示例：axios 拦截器
import axios from 'axios';

// 创建 axios 实例
const api = axios.create({
  baseURL: 'http://localhost:8080',
  withCredentials: true  // 重要：允许发送 Cookie（Session）
});

// 请求拦截器
api.interceptors.request.use(
  config => {
    // 可以在这里添加 token 等
    return config;
  },
  error => {
    return Promise.reject(error);
  }
);

// 响应拦截器
api.interceptors.response.use(
  response => {
    return response;
  },
  error => {
    if (error.response?.status === 401) {
      // 未登录，跳转到登录页
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);
```

### 5. 页面组件设计

#### 登录页面（Login.vue）

```vue
<template>
  <div class="login-container">
    <form @submit.prevent="handleLogin">
      <input v-model="username" placeholder="用户名" required />
      <input v-model="password" type="password" placeholder="密码" required />
      <button type="submit">登录</button>
      <router-link to="/register">注册账号</router-link>
    </form>
  </div>
</template>

<script>
export default {
  data() {
    return {
      username: '',
      password: ''
    };
  },
  methods: {
    async handleLogin() {
      try {
        await this.$store.dispatch('login', {
          username: this.username,
          password: this.password
        });
        
        // 根据角色跳转
        const role = this.$store.getters.userInfo.role;
        const routes = {
          'USER': '/user/dashboard',
          'STAFF': '/staff/dashboard',
          'AUDITOR': '/auditor/dashboard'
        };
        this.$router.push(routes[role] || '/login');
      } catch (error) {
        alert(error.message);
      }
    }
  }
};
</script>
```

#### 注册页面（Register.vue）

```vue
<template>
  <div class="register-container">
    <form @submit.prevent="handleRegister">
      <input v-model="username" placeholder="用户名" required />
      <input v-model="password" type="password" placeholder="密码" required />
      <input v-model="email" type="email" placeholder="邮箱（可选）" />
      <input v-model="phone" placeholder="手机号（可选）" />
      <button type="submit">注册</button>
      <router-link to="/login">已有账号？去登录</router-link>
    </form>
  </div>
</template>

<script>
export default {
  data() {
    return {
      username: '',
      password: '',
      email: '',
      phone: ''
    };
  },
  methods: {
    async handleRegister() {
      try {
        await api.post('/api/auth/register', {
          username: this.username,
          password: this.password,
          email: this.email,
          phone: this.phone
        });
        alert('注册成功，请登录');
        this.$router.push('/login');
      } catch (error) {
        alert(error.response?.data?.message || '注册失败');
      }
    }
  }
};
</script>
```

#### 不同角色的首页

**普通用户首页（UserDashboard.vue）**
- 显示组合产品列表
- 可以查看产品详情

**工作人员首页（StaffDashboard.vue）**
- 显示组合产品列表
- 可以创建新产品
- 可以编辑自己创建的产品

**审核人员首页（AuditorDashboard.vue）**
- 显示待审核的组合产品列表
- 可以审核产品（通过/拒绝）
- 可以查看所有产品

### 6. 导航栏设计

根据用户角色显示不同的导航菜单：

```vue
<template>
  <nav>
    <div v-if="userInfo">
      <span>欢迎，{{ userInfo.username }}（{{ userInfo.roleName }}）</span>
      
      <!-- 普通用户菜单 -->
      <template v-if="userInfo.role === 'USER'">
        <router-link to="/user/dashboard">首页</router-link>
      </template>
      
      <!-- 工作人员菜单 -->
      <template v-if="userInfo.role === 'STAFF'">
        <router-link to="/staff/dashboard">首页</router-link>
        <router-link to="/staff/portfolio/create">创建组合</router-link>
      </template>
      
      <!-- 审核人员菜单 -->
      <template v-if="userInfo.role === 'AUDITOR'">
        <router-link to="/auditor/dashboard">待审核</router-link>
        <router-link to="/staff/portfolio/create">创建组合</router-link>
      </template>
      
      <button @click="handleLogout">登出</button>
    </div>
    <div v-else>
      <router-link to="/login">登录</router-link>
      <router-link to="/register">注册</router-link>
    </div>
  </nav>
</template>

<script>
export default {
  computed: {
    userInfo() {
      return this.$store.getters.userInfo;
    }
  },
  methods: {
    async handleLogout() {
      await this.$store.dispatch('logout');
      this.$router.push('/login');
    }
  }
};
</script>
```

---

## 六、部署步骤

### 1. 执行数据库脚本

在数据库中执行 `src/main/resources/sql/user_auth_schema.sql` 创建用户表。

### 2. 启动后端服务

```bash
mvn spring-boot:run
```

### 3. 测试接口

使用 Postman 或 curl 测试登录接口：

```bash
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"user001","password":"123456"}' \
  -c cookies.txt
```

---

## 七、注意事项

1. **Session 配置**：确保前端请求时携带 Cookie（`withCredentials: true`）
2. **跨域配置**：后端已配置 CORS，允许前端域名访问
3. **密码安全**：密码使用 BCrypt 加密存储，不会明文保存
4. **角色管理**：普通用户注册时默认为 `USER` 角色，工作人员和审核人员需要管理员在数据库中手动修改
5. **权限控制**：后端已实现权限拦截，前端也需要做相应的路由守卫

---

## 八、常见问题

### Q1: 登录后 Session 丢失？

**A:** 检查前端请求是否设置了 `withCredentials: true`，确保 Cookie 被正确发送。

### Q2: 权限不足（403）？

**A:** 检查用户角色是否正确，以及接口路径是否符合权限要求。

### Q3: 如何创建工作人员或审核人员账号？

**A:** 可以先注册一个普通用户账号，然后在数据库中手动修改 `role` 字段为 `STAFF` 或 `AUDITOR`。

---

## 九、后续优化建议

1. **JWT Token 替代 Session**：如果需要在移动端或跨域场景使用，可以考虑使用 JWT
2. **角色管理界面**：添加管理员界面，可以修改用户角色
3. **密码重置功能**：添加忘记密码、重置密码功能
4. **用户资料管理**：允许用户修改个人信息
5. **操作日志**：记录用户的操作日志，便于审计

