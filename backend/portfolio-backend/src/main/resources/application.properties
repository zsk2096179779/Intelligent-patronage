server.port=8081
spring.application.name=AdvisoryCore
spring.datasource.url=jdbc:mysql://116.62.82.244:3306/wealthadvisor?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=remote_admin
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# MyBatis??
mybatis.mapper-locations=classpath:Mapper/*.xml
mybatis.type-aliases-package=com.example.train_back.entity

# Spring Session JDBC 配置（使用数据库存储会话）
spring.session.store-type=jdbc
# 会话超时时间（秒），默认30分钟
spring.session.timeout=1800
# 是否在启动时初始化数据库表（always=自动创建，never=不创建，需要手动执行SQL）
spring.session.jdbc.initialize-schema=always

pagehelper.helperDialect=mysql
pagehelper.reasonable=true
pagehelper.supportMethodsArguments=true
pagehelper.params=count=countSql

spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration

llm.provider.type=openai
llm.provider.url=https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions
llm.provider.apikey=sk-898eec52b88742af8e1bd45f42057a8d
llm.default.model=qwen-plus
# rate limit (requests per window) and window in seconds
llm.rate.limit=20
llm.rate.window.seconds=60

# Redis (可选) - 若启用 Redis, 项目会用 Redis 做分布式限流
# spring.redis.host=localhost
# spring.redis.port=6379
