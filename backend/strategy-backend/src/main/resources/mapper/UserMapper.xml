<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backend.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.example.backend.entity.User">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="password" column="password" />
        <result property="email" column="email" />
        <result property="phone" column="phone" />
        <result property="role" column="role" />
        <result property="stat" column="stat" />
        <result property="createTime" column="createtime" />
        <result property="updateTime" column="updatetime" />
    </resultMap>

    <select id="selectByUsername" parameterType="string" resultMap="UserResultMap">
        SELECT id, username, password, email, phone, role, status, created_at, updated_at
        FROM `users`
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="countByUsername" parameterType="string" resultType="int">
        SELECT COUNT(1) FROM `users` WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="com.example.backend.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `users` (username, password, email, phone, role, stat, createtime, updatetime)
        VALUES (#{username}, #{password}, #{email}, #{phone}, #{role}, #{status}, #{created_at}, #{updated_at})
    </insert>

</mapper>